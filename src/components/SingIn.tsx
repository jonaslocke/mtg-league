import { FC } from "react";
import Modal from "./Modal";

type Props = {};

const SignIn: FC<Props> = () => {
  return (
    <Modal>
      <div className="sign-in">test</div>
    </Modal>
  );
};

export default SignIn;
